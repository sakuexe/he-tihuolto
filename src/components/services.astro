---
// components
import SectionHeader from '../components/sectheader.tsx'
import Card from '../components/card.tsx'
// types
import type { Service } from '../components/card.tsx'
// data
import services from '../json/services.json'
---

<article id="services" class="sectioncontainer scroll-m-24">
  <SectionHeader topic="Palvelumme">
    Tarjoamme palvelut esilteltynä helposti seurattavassa listassa.
  </SectionHeader>
  <p>
    Tarjoamme laajan valikoiman kiinteistöhuoltopalveluita, jotka pitävät
    kiinteistönne parhaassa mahdollisessa kunnossa. Huolehdimme monipuolisesti
    esimerkiksi talotekniikasta ja siivouksesta lumitöihin ja vihertyöhön.
    Yrityksemme ammattitaito ja kokemus takaavat, että jokainen palvelu
    suoritetaan huolellisesti ja tehokkaasti, jättäen taaksemme tyytyväisiä
    asiakkaita ja hyvin hoidettuja kiinteistöjä.
  </p>
  <div class="flex flex-wrap justify-center gap-5">
    {
      services.map((service: Service, index: number) => (
        <Card service={service} index={index} />
      ))
    }
  </div>
</article>
